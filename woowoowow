if not game:IsLoaded() then
	game.Loaded:Wait()
end

-- URL of the raw Gist content
local gistUrl = "https://raw.githubusercontent.com/Zxlono/varsitydhc/main/key"

-- Function to fetch and check the key
local function checkKey()
	local validKeysScript = game:HttpGet(gistUrl)
	local validKeys = loadstring(validKeysScript)()

	-- Check if the provided key is in the list of valid keys
	local isValid = false
	for _, validKey in pairs(validKeys) do
		if getgenv().key == validKey then
			isValid = true
			break
		end
	end

	return isValid
end

-- Execute actions based on key validity
if checkKey() then
	print("Key is valid.")
	-- Run the script for a valid key
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local Players = game:GetService("Players")
	local LocalPlayer = Players.LocalPlayer
	local MainScreenGui = LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild("MainScreenGui", true)

	local function Attack()
		if LocalPlayer.Backpack:FindFirstChild("Combat") then
			LocalPlayer.Backpack.Combat.Parent = LocalPlayer.Character
		end

		local Combat = LocalPlayer.Character:FindFirstChild("Combat")
		if not Combat then
			print("No combat tool :(")
			return
		end

		Combat:Activate()
		wait(0.1) -- Adjust based on your game's mechanics
		Combat:Deactivate()
	end

	local function buyBoxingMoveset()
		local movesetShop = game.Workspace.Ignored.Shop:FindFirstChild("Boxing Moveset (Require: Max Box Stat) - $0")
		if movesetShop and movesetShop:FindFirstChild("ClickDetector") then
			fireclickdetector(movesetShop.ClickDetector)
			print("Attempted to buy the Boxing Moveset.")
		else
			warn("Boxing Moveset shop or ClickDetector not found.")
		end
	end

	local function StartBoxing()
		local BoxingValueObject = LocalPlayer.DataFolder.Information:FindFirstChild("BoxingValue")
		local MaxBoxingValue = ReplicatedStorage:FindFirstChild("MaxBoxing")

		-- Create BoxingValueObject if it doesn't exist to handle zero progress scenario
		if not BoxingValueObject then
			BoxingValueObject = Instance.new("IntValue")
			BoxingValueObject.Value = 0
		end

		if not MaxBoxingValue then
			warn("MaxBoxing not found.")
			return
		end

		if BoxingValueObject.Value / MaxBoxingValue.Value >= 1 then
			print("Boxing bar is already filled.")
			buyBoxingMoveset()
			loadstring(game:HttpGet('https://raw.githubusercontent.com/Zxlono/varsitydhc/main/autofarm2'))()
			return
		end

		LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-251, 22, -1137)

		repeat
			wait(0.1)
			Attack()
			-- Refresh the boxing value to ensure it's up to date
			BoxingValueObject.Value = LocalPlayer.DataFolder.Information:FindFirstChild("BoxingValue") and LocalPlayer.DataFolder.Information.BoxingValue.Value or 0
		until BoxingValueObject.Value / MaxBoxingValue.Value >= 1

		buyBoxingMoveset()
		loadstring(game:HttpGet('https://raw.githubusercontent.com/Zxlono/varsitydhc/main/autofarm2'))()
	end

	StartBoxing()
	local player = game.Players.LocalPlayer

	-- Function to buy the key
	local function buyKey()
		local keyShop = game.Workspace.Ignored.Shop:FindFirstChild("[Key] - $133")
		if keyShop and keyShop:FindFirstChild("ClickDetector") then
			fireclickdetector(keyShop.ClickDetector)
		else
			warn("Key shop or ClickDetector not found.")
		end
	end

	-- Function to show the key with retries
	local function showKey(retryCount)
		local key = player.Backpack:FindFirstChild("[Key]")
		if key then
			key.Parent = player.Character
		elseif retryCount > 0 then
			wait(1) -- Wait for a bit before retrying
			showKey(retryCount - 1) -- Retry showing the key
		else
			warn("[Key] could not be shown after retries.")
		end
	end

	-- Proximity and direction check setup
	local targetCFrame = CFrame.new(-294.648, 21.758, -121.547) -- Target CFrame for detection
	local detectionRadius = 90 -- Detection radius around the target CFrame
	local hasTeleported = false -- Flag to prevent repeated teleportation and actions

	game:GetService("RunService").Heartbeat:Connect(function()
		if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
			local playerPosition = player.Character.HumanoidRootPart.Position
			local distanceToTarget = (playerPosition - targetCFrame.Position).magnitude

			-- Check if the player is within the detection radius
			if distanceToTarget <= detectionRadius then
				local playerDirection = player.Character.HumanoidRootPart.CFrame.LookVector
				local directionToTarget = (targetCFrame.Position - playerPosition).unit
				local dotProduct = playerDirection:Dot(directionToTarget)

				-- Check if the player is facing the target direction (dot product > 0 means facing towards)
				if dotProduct > 0 and not hasTeleported then
					player.Character.HumanoidRootPart.CFrame = CFrame.new(-270.911, 21.748, -238.997) -- Teleport to key buying location
					wait(0.5) -- Ensure the game registers the player's new position
					buyKey()
					wait(0.5) -- Wait for the key purchase to process
					showKey(3) -- Attempt to show the key with up to 3 retries
					hasTeleported = true -- Prevent repeated actions
				end
			else
				hasTeleported = false -- Reset when the player moves away from the detection area
			end
		end
	end)
	
	local function handleRedeemCommand(code)
		local localPlayer = game.Players.LocalPlayer
		game:GetService("ReplicatedStorage").MainEvent:FireServer("EnterPromoCode", code)
	end

	for _, code in ipairs(getgenv().codes) do
		handleRedeemCommand(code)
	end
	
	local function controlFPS()
		local RunService = game:GetService("RunService")
		while true do
			local t0 = tick()
			RunService.Heartbeat:Wait()
			repeat until (t0 + 1/getgenv().fps) < tick()
		end
	end

	spawn(controlFPS)
	
	-- Gui to Lua
	-- Version: 3.2

	-- Instances:

	local AutoFarmScreen = Instance.new("ScreenGui")
	local BackGroundBlack = Instance.new("Frame")
	local CashEarnedLabel = Instance.new("TextLabel")
	local UICorner = Instance.new("UICorner")
	local StartingCashLabel = Instance.new("TextLabel")
	local CurrentCashLabel = Instance.new("TextLabel")
	local UICorner_2 = Instance.new("UICorner")
	local StartingCashLabel_2 = Instance.new("TextLabel")
	local StartCashLabel = Instance.new("TextLabel")
	local UICorner_3 = Instance.new("UICorner")
	local StartingCashLabel_3 = Instance.new("TextLabel")
	local Display = Instance.new("TextLabel")
	local Username = Instance.new("TextLabel")
	local UIAspectRatioConstraint = Instance.new("UIAspectRatioConstraint")
	local VersionLabel = Instance.new("TextLabel")
	local UIAspectRatioConstraint_2 = Instance.new("UIAspectRatioConstraint")
	local CreditLabel = Instance.new("TextLabel")
	local UIAspectRatioConstraint_3 = Instance.new("UIAspectRatioConstraint")
	local TotalTime = Instance.new("TextLabel")
	local UICorner_4 = Instance.new("UICorner")
	local TimePlayedLabel = Instance.new("TextLabel")
	local FPSLabel = Instance.new("TextLabel")
	local UIAspectRatioConstraint_4 = Instance.new("UIAspectRatioConstraint")

	--Properties:

	AutoFarmScreen.Name = "AutoFarmScreen"
	AutoFarmScreen.Parent = game.CoreGui
	AutoFarmScreen.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

	BackGroundBlack.Name = "BackGroundBlack"
	BackGroundBlack.Parent = AutoFarmScreen
	BackGroundBlack.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	BackGroundBlack.BorderColor3 = Color3.fromRGB(0, 0, 0)
	BackGroundBlack.BorderSizePixel = 0
	BackGroundBlack.Position = UDim2.new(-0.275000006, 0, -0.328000009, 0)
	BackGroundBlack.Size = UDim2.new(1.41999996, 0, 1.68599999, 0)

	CashEarnedLabel.Name = "CashEarnedLabel"
	CashEarnedLabel.Parent = BackGroundBlack
	CashEarnedLabel.BackgroundColor3 = Color3.fromRGB(43, 43, 43)
	CashEarnedLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
	CashEarnedLabel.BorderSizePixel = 0
	CashEarnedLabel.Position = UDim2.new(0.544703066, 0, 0.496296197, 0)
	CashEarnedLabel.Size = UDim2.new(0, 115, 0, 25)
	CashEarnedLabel.Font = Enum.Font.Unknown
	CashEarnedLabel.Text = "Loading..."
	CashEarnedLabel.TextColor3 = Color3.fromRGB(60, 255, 0)
	CashEarnedLabel.TextSize = 12.000

	UICorner.CornerRadius = UDim.new(0, 6)
	UICorner.Parent = CashEarnedLabel

	StartingCashLabel.Name = "StartingCashLabel"
	StartingCashLabel.Parent = CashEarnedLabel
	StartingCashLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	StartingCashLabel.BackgroundTransparency = 1.000
	StartingCashLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
	StartingCashLabel.BorderSizePixel = 0
	StartingCashLabel.Position = UDim2.new(-0.769130528, 0, -0.0130004883, 0)
	StartingCashLabel.Size = UDim2.new(0, 73, 0, 24)
	StartingCashLabel.Font = Enum.Font.Unknown
	StartingCashLabel.Text = "Cash Earned:"
	StartingCashLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	StartingCashLabel.TextSize = 12.000

	CurrentCashLabel.Name = "CurrentCashLabel"
	CurrentCashLabel.Parent = BackGroundBlack
	CurrentCashLabel.BackgroundColor3 = Color3.fromRGB(43, 43, 43)
	CurrentCashLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
	CurrentCashLabel.BorderSizePixel = 0
	CurrentCashLabel.Position = UDim2.new(0.544703066, 0, 0.559403419, 0)
	CurrentCashLabel.Size = UDim2.new(0, 115, 0, 25)
	CurrentCashLabel.Font = Enum.Font.Unknown
	CurrentCashLabel.Text = "Loading..."
	CurrentCashLabel.TextColor3 = Color3.fromRGB(60, 255, 0)
	CurrentCashLabel.TextSize = 12.000

	UICorner_2.CornerRadius = UDim.new(0, 6)
	UICorner_2.Parent = CurrentCashLabel

	StartingCashLabel_2.Name = "StartingCashLabel"
	StartingCashLabel_2.Parent = CurrentCashLabel
	StartingCashLabel_2.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	StartingCashLabel_2.BackgroundTransparency = 1.000
	StartingCashLabel_2.BorderColor3 = Color3.fromRGB(0, 0, 0)
	StartingCashLabel_2.BorderSizePixel = 0
	StartingCashLabel_2.Position = UDim2.new(-0.769130528, 0, -0.0130004883, 0)
	StartingCashLabel_2.Size = UDim2.new(0, 73, 0, 24)
	StartingCashLabel_2.Font = Enum.Font.Unknown
	StartingCashLabel_2.Text = "Current Cash:"
	StartingCashLabel_2.TextColor3 = Color3.fromRGB(255, 255, 255)
	StartingCashLabel_2.TextSize = 12.000

	StartCashLabel.Name = "StartCashLabel"
	StartCashLabel.Parent = BackGroundBlack
	StartCashLabel.BackgroundColor3 = Color3.fromRGB(43, 43, 43)
	StartCashLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
	StartCashLabel.BorderSizePixel = 0
	StartCashLabel.Position = UDim2.new(0.544748843, 0, 0.433261037, 0)
	StartCashLabel.Size = UDim2.new(0, 115, 0, 25)
	StartCashLabel.Font = Enum.Font.Unknown
	StartCashLabel.Text = "Loading..."
	StartCashLabel.TextColor3 = Color3.fromRGB(60, 255, 0)
	StartCashLabel.TextSize = 12.000

	UICorner_3.CornerRadius = UDim.new(0, 6)
	UICorner_3.Parent = StartCashLabel

	StartingCashLabel_3.Name = "StartingCashLabel"
	StartingCashLabel_3.Parent = StartCashLabel
	StartingCashLabel_3.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	StartingCashLabel_3.BackgroundTransparency = 1.000
	StartingCashLabel_3.BorderColor3 = Color3.fromRGB(0, 0, 0)
	StartingCashLabel_3.BorderSizePixel = 0
	StartingCashLabel_3.Position = UDim2.new(-0.829904318, 0, -0.012727051, 0)
	StartingCashLabel_3.Size = UDim2.new(0, 80, 0, 24)
	StartingCashLabel_3.Font = Enum.Font.Unknown
	StartingCashLabel_3.Text = "Starting Cash:"
	StartingCashLabel_3.TextColor3 = Color3.fromRGB(255, 255, 255)
	StartingCashLabel_3.TextSize = 12.000

	Display.Name = "Display"
	Display.Parent = BackGroundBlack
	Display.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	Display.BackgroundTransparency = 1.000
	Display.BorderColor3 = Color3.fromRGB(0, 0, 0)
	Display.BorderSizePixel = 0
	Display.Position = UDim2.new(0.501807928, 0, 0.34120959, 0)
	Display.Size = UDim2.new(0.0949343666, 0, 0.0308278371, 0)
	Display.Font = Enum.Font.Unknown
	Display.Text = "PlayerDisplay"
	Display.TextColor3 = Color3.fromRGB(255, 255, 255)
	Display.TextSize = 21.000

	Username.Name = "Username"
	Username.Parent = BackGroundBlack
	Username.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	Username.BackgroundTransparency = 1.000
	Username.BorderColor3 = Color3.fromRGB(0, 0, 0)
	Username.BorderSizePixel = 0
	Username.Position = UDim2.new(0.48446849, 0, 0.372451693, 0)
	Username.Size = UDim2.new(0.149645388, 0, 0.0538687594, 0)
	Username.Font = Enum.Font.Unknown
	Username.Text = "PlayerUser"
	Username.TextColor3 = Color3.fromRGB(255, 255, 255)
	Username.TextSize = 15.000

	UIAspectRatioConstraint.Parent = Username
	UIAspectRatioConstraint.AspectRatio = 3.836

	VersionLabel.Name = "VersionLabel"
	VersionLabel.Parent = BackGroundBlack
	VersionLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	VersionLabel.BackgroundTransparency = 1.000
	VersionLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
	VersionLabel.BorderSizePixel = 0
	VersionLabel.Position = UDim2.new(0.233858153, 0, 0.69890666, 0)
	VersionLabel.Size = UDim2.new(0.149645388, 0, 0.0538687594, 0)
	VersionLabel.Font = Enum.Font.Unknown
	VersionLabel.Text = "Version: 1.0"
	VersionLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	VersionLabel.TextSize = 11.000

	UIAspectRatioConstraint_2.Parent = VersionLabel
	UIAspectRatioConstraint_2.AspectRatio = 3.836

	CreditLabel.Name = "CreditLabel"
	CreditLabel.Parent = BackGroundBlack
	CreditLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	CreditLabel.BackgroundTransparency = 1.000
	CreditLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
	CreditLabel.BorderSizePixel = 0
	CreditLabel.Position = UDim2.new(0.233858153, 0, 0.731127143, 0)
	CreditLabel.Size = UDim2.new(0.149645388, 0, 0.0538687594, 0)
	CreditLabel.Font = Enum.Font.Unknown
	CreditLabel.Text = "MADE BY KOBE0921"
	CreditLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	CreditLabel.TextSize = 11.000

	UIAspectRatioConstraint_3.Parent = CreditLabel
	UIAspectRatioConstraint_3.AspectRatio = 3.836

	TotalTime.Name = "TotalTime"
	TotalTime.Parent = BackGroundBlack
	TotalTime.BackgroundColor3 = Color3.fromRGB(43, 43, 43)
	TotalTime.BorderColor3 = Color3.fromRGB(0, 0, 0)
	TotalTime.BorderSizePixel = 0
	TotalTime.Position = UDim2.new(0.544703066, 0, 0.621214151, 0)
	TotalTime.Size = UDim2.new(0, 115, 0, 25)
	TotalTime.Font = Enum.Font.Unknown
	TotalTime.Text = "Loading..."
	TotalTime.TextColor3 = Color3.fromRGB(255, 255, 255)
	TotalTime.TextSize = 12.000

	UICorner_4.CornerRadius = UDim.new(0, 6)
	UICorner_4.Parent = TotalTime

	TimePlayedLabel.Name = "TimePlayedLabel"
	TimePlayedLabel.Parent = TotalTime
	TimePlayedLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	TimePlayedLabel.BackgroundTransparency = 1.000
	TimePlayedLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
	TimePlayedLabel.BorderSizePixel = 0
	TimePlayedLabel.Position = UDim2.new(-0.769130528, 0, -0.0130004883, 0)
	TimePlayedLabel.Size = UDim2.new(0, 73, 0, 24)
	TimePlayedLabel.Font = Enum.Font.Unknown
	TimePlayedLabel.Text = "Time Elapsed:"
	TimePlayedLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	TimePlayedLabel.TextSize = 12.000

	FPSLabel.Name = "FPSLabel"
	FPSLabel.Parent = BackGroundBlack
	FPSLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	FPSLabel.BackgroundTransparency = 1.000
	FPSLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
	FPSLabel.BorderSizePixel = 0
	FPSLabel.Position = UDim2.new(0.233858153, 0, 0.66537106, 0)
	FPSLabel.Size = UDim2.new(0.149645388, 0, 0.0538687594, 0)
	FPSLabel.Font = Enum.Font.Unknown
	FPSLabel.Text = "FPS:"
	FPSLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	FPSLabel.TextSize = 11.000

	UIAspectRatioConstraint_4.Parent = FPSLabel
	UIAspectRatioConstraint_4.AspectRatio = 3.836

	-- Scripts:

	local function VAER_fake_script() -- CashEarnedLabel.CashEarnedScript 
		local script = Instance.new('LocalScript', CashEarnedLabel)

		wait(1.5)

		local player = game.Players.LocalPlayer
		local currencyTextLabel = script.Parent
		local totalCurrency = player:WaitForChild("DataFolder").Currency.Value

		-- Function to update the profit display
		local function updateProfitDisplay()
			local currentCurrency = player:WaitForChild("DataFolder").Currency.Value
			local currencyChange = currentCurrency - totalCurrency

			if currencyChange > 0 then
				currencyTextLabel.Text = "+$" .. currencyChange
			else
				currencyTextLabel.Text = "+$0"
			end
		end

		-- Connect the update function to changes in currency
		player:WaitForChild("DataFolder").Currency.Changed:Connect(updateProfitDisplay)

		-- Update the display initially
		updateProfitDisplay()

	end
	coroutine.wrap(VAER_fake_script)()
	local function ZJNZPX_fake_script() -- CurrentCashLabel.CurrentCashScript 
		local script = Instance.new('LocalScript', CurrentCashLabel)

		wait(1.5)

		local player = game.Players.LocalPlayer
		local currencyTextLabel = script.Parent
		local totalCurrency = player:WaitForChild("DataFolder").Currency.Value

		-- Function to update the currency display
		local function updateCurrencyDisplay()
			local currentCurrency = player:WaitForChild("DataFolder").Currency.Value
			local currencyChange = currentCurrency - totalCurrency

			totalCurrency = totalCurrency + currencyChange

			local sign = (totalCurrency >= 0) and "" or ""
			currencyTextLabel.Text = "$" .. sign .. totalCurrency
		end

		-- Connect the update function to changes in currency
		player:WaitForChild("DataFolder").Currency.Changed:Connect(updateCurrencyDisplay)

		-- Update the display initially
		updateCurrencyDisplay()
	end
	coroutine.wrap(ZJNZPX_fake_script)()
	local function UZFNDYB_fake_script() -- StartCashLabel.StartCashScript 
		local script = Instance.new('LocalScript', StartCashLabel)

		wait(1.5)

		local player = game.Players.LocalPlayer
		local dataFolder = player:WaitForChild("DataFolder")
		local currencyTextLabel = script.Parent

		-- Display the player's starting currency in the TextLabel
		currencyTextLabel.Text = "$" .. tostring(dataFolder.Currency.Value)
	end
	coroutine.wrap(UZFNDYB_fake_script)()
	local function DYAS_fake_script() -- Display.LocalScript 
		local script = Instance.new('LocalScript', Display)

		local frame = script.Parent

		local player = game.Players.LocalPlayer

		frame.Text = "" .. player.DisplayName
	end
	coroutine.wrap(DYAS_fake_script)()
	local function IRLFZI_fake_script() -- Username.LocalScript 
		local script = Instance.new('LocalScript', Username)

		local frame = script.Parent

		local player = game.Players.LocalPlayer

		frame.Text = "" .. player.Name
	end
	coroutine.wrap(IRLFZI_fake_script)()
	local function WQIT_fake_script() -- TotalTime.TotalTimeScript 
		local script = Instance.new('LocalScript', TotalTime)

		local textLabel = script.Parent -- Assume the script is placed directly inside the TextLabel

		local startTime = os.time()

		-- Function to update the time display
		local function updateDisplay()
			local currentTime = os.time() - startTime

			local days = math.floor(currentTime / (60 * 60 * 24))
			local hours = math.floor((currentTime % (60 * 60 * 24)) / (60 * 60))
			local minutes = math.floor((currentTime % (60 * 60)) / 60)
			local seconds = currentTime % 60

			textLabel.Text = string.format("%dd %dh %dm %ds", days, hours, minutes, seconds)
		end

		-- Update the display every second
		while wait(1) do
			updateDisplay()
		end
	end
	coroutine.wrap(WQIT_fake_script)()
	local function RTMARI_fake_script() -- BackGroundBlack.PlayerImageScript 
		local script = Instance.new('LocalScript', BackGroundBlack)

		local frame = script.Parent



		local player = game.Players.LocalPlayer



		local userId = player.UserId
		local thumbType = Enum.ThumbnailType.AvatarBust
		local thumbSize = Enum.ThumbnailSize.Size180x180

		local content, isReady = game.Players:GetUserThumbnailAsync(userId, thumbType, thumbSize)

		frame.PlayerImage.Image = content
	end
	coroutine.wrap(RTMARI_fake_script)()
	local function BFFNWH_fake_script() -- FPSLabel.FPSScript 
		local script = Instance.new('LocalScript', FPSLabel)

		local RunService = game:GetService('RunService')
		local framesloaded = 0

		RunService.RenderStepped:Connect(function()
			framesloaded += 1
		end)

		while true do
			wait(1)
			script.Parent.Text = "FPS: " .. framesloaded
			framesloaded = 0
		end
	end
	coroutine.wrap(BFFNWH_fake_script)()
	
	game:GetService("RunService"):Set3dRenderingEnabled(false)
	settings().Rendering.QualityLevel = 1
	UserSettings().GameSettings.MasterVolume = 0
	
	local decalsyeeted = true -- Leaving this on makes games look shitty but the fps goes up by at least 20.
	local g = game
	local w = g.Workspace
	local l = g.Lighting
	local t = w.Terrain
	t.WaterWaveSize = 0
	t.WaterWaveSpeed = 0
	t.WaterReflectance = 0
	t.WaterTransparency = 0
	l.GlobalShadows = false
	l.FogEnd = 9e9
	l.Brightness = 0
	settings().Rendering.QualityLevel = "Level01"
	for i, v in pairs(g:GetDescendants()) do
		if v:IsA("Part") or v:IsA("Union") or v:IsA("CornerWedgePart") or v:IsA("TrussPart") then
			v.Material = "Plastic"
			v.Reflectance = 0
		elseif v:IsA("Decal") or v:IsA("Texture") and decalsyeeted then
			v.Transparency = 1
		elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
			v.Lifetime = NumberRange.new(0)
		elseif v:IsA("Explosion") then
			v.BlastPressure = 1
			v.BlastRadius = 1
		elseif v:IsA("Fire") or v:IsA("SpotLight") or v:IsA("Smoke") or v:IsA("Sparkles") then
			v.Enabled = false
		elseif v:IsA("MeshPart") then
			v.Material = "Plastic"
			v.Reflectance = 0
			v.TextureID = 10385902758728957
		end
	end
	for i, e in pairs(l:GetChildren()) do
		if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("ColorCorrectionEffect") or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then
			e.Enabled = false
		end
	end

else
	print("Key is not valid.")
	-- Kick the player for an invalid key
	game.Players.LocalPlayer:Kick("INVALID KEY")
end
